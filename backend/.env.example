# Configuration Email (OBLIGATOIRE)
# Utilisez votre adresse Gmail et un mot de passe d'application
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Configuration SMTP (Optionnel - Gmail par défaut)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587

# Configuration de l'entreprise
SUPPORT_EMAIL=support@neolons.com
COMPANY_NAME=Neolons

# Configuration des notifications
NOTIFICATION_ENABLED=true
REMINDER_DAYS=7,3,1

# Configuration MongoDB (Optionnel - localhost par défaut)
MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_DATABASE=Neo_TM_DB

# Configuration du serveur (Optionnel - 8090 par défaut)
SERVER_PORT=8090

# Instructions pour Gmail:
# 1. Activez l'authentification à 2 facteurs sur votre compte Google
# 2. Allez dans les paramètres de sécurité Google
# 3. Sélectionnez "Mots de passe des applications"
# 4. Générez un mot de passe pour "Mail"
# 5. Utilisez ce mot de passe dans MAIL_PASSWORD (pas votre mot de passe principal)

# Pour utiliser ce fichier:
# 1. Copiez ce fichier vers .env: cp .env.example .env
# 2. Modifiez les valeurs dans .env avec vos vraies informations
# 3. Ne commitez jamais le fichier .env (il contient des informations sensibles)

